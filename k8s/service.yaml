apiVersion: v1
kind: Service
metadata:
  name: maestria-inventario-products
  namespace: actividad1
spec:
  type: LoadBalancer
  selector:
    app: maestria-inventario-products
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 9090
